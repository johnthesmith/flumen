# Дисклеймер

[Первая статья](file:///home/still/downloads/flumen_ru.pdf) вводила базовую идею 
флюменов как чисто топологических объектов. В процессе осмысления модели 
потребовалось уточнение базовых понятий, что привело к появлению данной работы.

Настоящая статья расширяет определение флюмена, добавляя атрибут — целое 
неотрицательное число, интерпретируемое как количество информации. Это 
минимальное дополнение, позволяющее перейти от чистой топологии к динамике.

Текущая статья — не законченная теория, а рассуждение, фиксирующее промежуточный 
этап развития модели. Это вторая версия, которая:

1. уточняет базовые понятия;
2. формулирует минимальные правила распространения информации;

Данная работа — пояснительный этап рассуждений. В процессе размышлений выявлена 
необходимость следующей версии, однако для демонстрации логики развития модели и 
фиксации текущего состояния публикуется эта статья.



# Дисклеймер

Статья не претендует на «теорию всего» для описания физической 
реальности. Это игровая песочница — формальная система, цель которой — 
посмотреть, какие структуры могут возникать из плоского списка элементов с 
минимальными правилами.

Любые совпадения с физическими терминами носят исключительно иллюстративный 
характер и не должны восприниматься как попытка описать реальные физические 
процессы.



# Определения

Начинаем с определений.



## Информация

Информация — дискретная величина, представленная целым неотрицательным числом 
```
w ∈ ℕ₀
```
Минимальная единица информации — квант (w = 1), неделимый и при 
распространении перемещающийся как целое. Информация может накапливаться, 
делиться на кванты и перераспределяться.


## Метка

Метка — абстрактный уникальный идентификатор, элемент счетного множества 𝕃. 
Метки не имеют внутренней структуры, не упорядочены и не наделены числовыми 
свойствами. Они служат исключительно для различения объектов и установления 
связей между флюменами.



## Флюмен

> ⚠️  Переопределение понятия, введенного в [первой 
> статье](file:///home/still/downloads/flumen_ru.pdf). Далее используется 
> следующее определение:

Флюмен — абстрактная однонаправленная связь между двумя метками α, β ∈ 𝕃, 
несущая целое неотрицательное количество информации w ∈ ℕ₀.


### Обозначение

```
f[a, w, b]
```

1. a ∈ 𝕃 — метка начала
2. b ∈ 𝕃 — метка конца
3. w ∈ ℕ₀ — количество информации

> ℹ️  В примерах для наглядности могут использоваться композитные буквенные и 
> индексные обозначения (a, b, c, x₀, t₁), но они всегда являются элементами 𝕃 и 
> не несут числового смысла. Флюмен не имеет координат и не вложен в 
> пространство.


### Свойства флюмена

1. Ориентированность — направление связи существенно: флюмен `f[a, w, b]` не 
равен флюмену `f[b, w, a]`.
2. Композиционность — флюмены соединяются в структуры через совпадающие метки. 
Метка `b` одного флюмена может совпадать с меткой `a` другого, образуя 
последовательности.
3. Квантованность — флюмен неделим как целое. Метки `a` и `b` являются 
неотъемлемой частью флюмена. Флюмен существует только как полная тройка 
`[a,w,b]`.



## Множество флюменов

1. Состояние системы — конечное множество флюменов. 

```
𝔽 = { f₁[a₁, w₁, b₁], f₂[a₂, w₂, b₂], …, fₙ[aₙ, wₙ, bₙ] }
```

### Свойства множества

1. Полнота — множество содержит все флюмены, существующие в данный момент 
рассмотрения.
0. Дискретность — множество конечно.

### Примечание
Множество флюменов — это не пространство и не граф. Это плоский список 
элементов.

> ❓ Уникальность — допистимо ли сущетсование двух флюменов с одинаковыми 
> метками `a` и `b`.



## Функция над флюменами

Функция `Φ` распротсранения информации во множестве флюменов принимает состояние `𝔽` 
и возвращает состояние `𝔽'`.

```
Φ: 𝔽 → 𝔽'
```

### Назначение

Функция определяет, как изменяется распределение информации по 
флюменам — веса w перераспределяются в соответствии с заданными правилами, 
состав множества может оставаться неизменным или меняться (если функция включает 
операции создания и удаления флюменов).

Статья декларирует два подхода к распространению информации, соответствующих 
двум взглядам на функцию `Φ`:

1. [D - Динамическая флюмен-функция](#динамическая-флюмен-функция)
0. [S - Статическая флюмен-функция](#статическая-флюмен-функция)

Оба подхода математически эквивалентны. Динамическая функция `S` порождает 
последовательность, которая удовлетворяет статической функции `D`. Выбор зависит 
от задачи и интерпретации.

Динамическая функция `S` может рассматриваться как способ построения 
последовательности, удовлетворяющей статической функции `D`. В этом смысле 
динамика — частный случай статического описания, где время появляется как 
параметр упорядочивания срезов.



#### Динамическая флюмен-функция

Функция `D` производит процесс последовательного изменения состояний множества 
флюменов. Каждое применение функции порождает новое состояние, при этом 
происходит единовременная обработка всех флюменов.

```
D(𝔽ₜ) = 𝔽ₜ₊₁
```

Последовательность состояний:

```
𝔽₀ → 𝔽₁ → 𝔽₂ → …
```

Каждое состояние 𝔽ₜ содержит срез — мгновенную конфигурацию распределения 
информации по флюменам. Последовательность срезов создает иллюзию течения 
времени.



#### Статическая флюмен-функция

Функция `S` задает правило согласованного распределения информации по всем 
флюменам множества. Все состояния 𝔽₀, 𝔽₁, …, 𝔽ₙ существуют одновременно, функция 
`S` определяет связанное расределение между ними.

```
D(𝔽ₜ, 𝔽ₜ₊₁) = истина
```
`S` применяется ко множеству `F` единожды, при этом итерационно 
распространяет информацию между флюменами вплоть до завершения распространения 
информации.



### Аппарат флюмен-функций

Для флюмен-функций необходимо перечислить
[общие](#общие-правила-флюмен-функций) и 
[различные](#различные-правила-флюмен-функций) правила распространения 
информации.

#### Общие правила флюмен функций

Для обоих `S` и `D` флюмен-функций определим следующие правила:

1. `Локальность` — информация передается только между соседними флюменами: флюмен 
может принять информацию из другого флюмена, если метка конца первого совпадает с 
меткой начала второго. Перескоки через промежуточные флюмены запрещены.
2. `Квантовость` — информация передается только целыми единицами (квантами). 
Минимальная порция передачи — один квант (`w` = 1). Информация не может дробиться 
на части при распространении.
3. `Причинность` — информация всегда распространяется по направлению флюмена: от 
метки начала к метке конца. Обратное движение невозможно.
4. `Сохранение` - для любой метки `x` сумма информации во входящих флюменнах 
равна 
```
∑ w(f[*, *, x]) = ∑ w(f[x, *, *])
```
при этом сумма входящей информации суммируется с имеющийся информацией во 
флюмене приемнике, те
```
w_j(t+1) = w_j(t) + ∑_{i: b_i = a_j} δ_{i→j}
```
где δ_{i→j} ∈ {0,1} — факт передачи кванта из флюмена f_i в флюмен f_j.


#### Различные правила флюмен функций правила флюмен функций

При использовании динамической функции `D` информация обнуляется во 
флюмене-источнике и записывается во флюмен-приемник. Прошлое состояние не 
сохраняется — каждый очередной расчет порождает новый срез, замещая предыдущий. 
Общая сумма информации во множестве флюменов остается неизменной на каждом шаге.

При использовании статической функции `S` информация сохраняется в источнике и 
добавляет ее копию в приемник. В результате все промежуточные состояния 𝔽₀, 𝔽₁, 
…, 𝔽ₙ существуют одновременно как единый блок.

Для образности попробуем сказать что `D` — проектор, показывающий фильм: виден 
только текущий кадр, прошлые исчезают. Статическая функция S — кинолента, где 
все кадры существуют одновременно.



# Эмуляция "одиночесво"

1. Теперь, имея формальный аппарат, посмотрим, какие эмерджентные эффекты 
возникают в простых конфигурациях.

Определим один флюмен с минимальным квантом информации:

```
f1[ a, 1, b ]
```

Данная конфигурация неизменна: квант информации находится во флюмене, но не 
может распространицться, так как нет других флюменов, связанных с метками `a` 
или `b`. Это простейший «атом» пространства — зародыш будущих структур.



# Эмуляция распространения информации

2. Обнулим наш 'F' и определним флюмены:

```
f1[ a, 1, b ],
f2[ b, 0, c ], 
f3[ c, 0, d ]
```

в результате можно говорить о графе вида:

```
a-1->b-0->c-0->d
```

## Поведение динамической функции D

При последовательном применении D наблюдаем перемещение кванта:

```
Шаг 0: a-1->b-0->c-0->d
Шаг 1: a-0->b-1->c-0->d
Шаг 2: a-0->b-0->c-1->d
```

Что демонстрирует:

1. Движение — квант последовательно переходит от флюмена к флюмену
0. Необратимость — вернуться в предыдущее состояние невозможно
0. Дискретность — каждый шаг переносит ровно один квант
0. Локальность — передача только между соседями

и статическую `S`


## Поведение статической функции S

При применении S все состояния фиксируются одновременно:

```
Шаг 0: a-1->b-0->c-0->d
Шаг 1: a-1->b-1->c-0->d
Шаг 2: a-1->b-1->c-1->d
```

Что демонстрирует:

1. Накопление — квант копируется, а не перемещается
2. Сосуществование — все положения кванта присутствуют одновременно
3. Траектория как целое — мировая линия частицы.



## Эмерджентные свойства время и пространство

Из выше приведенной простейшей конфигурации возникают следующие понятия как 
эмерджентные свойства:

1. [Время](#время)
2. [Пространство](#простраснтво)



## Время

В модели флюменов время не первично, а возникает из способа рассмотрения 
последовательности срезов.

В динамическом подходе `D` время — номер шага, индекс среза в последовательности 
𝔽₀ → 𝔽₁ → 𝔽₂ → …. Каждый шаг порождает новый срез, замещая предыдущий — 
возникает иллюзия течения времени.

В статическом подходе `S` время — отношение порядка между одновременно 
существующими срезами. Все `𝔽₀, 𝔽₁, …, 𝔽ₙ` фиксированы, а `S(𝔽ₜ, 𝔽ₜ₊₁) = истина`
задает направление от "ранних" к "поздним".

Общее: время сводится к упорядочиванию срезов. Оно не заложено в элементах 
модели, а возникает из структуры связей между срезами и выбранного взгляда на 
них.




## Пространство

В модели флюменов пространство возникает из связности, а не заложено в 
элементах.

В цепочке `a -1→ b -0→ c -0→ d` метки `a`, `b`, `c`, `d` — не координаты, а 
идентификаторы. Порядок следования флюменов задает линейную структуру, 
воспринимаемую как одномерное пространство.

Важно: в данной цепочке каждая метка представляет одну и ту же точку в разные 
моменты времени. Это траектория точки, а не набор разных точек в пространстве.

Пространство как таковое возникает только при наличии альтернативных путей — 
когда из одной метки можно перейти в разные следующие. В чистой цепочке мы видим 
лишь временную эволюцию.

Таким образом, пространство — структура возможных переходов между состояниями, 
зафиксированная в топологии флюменов.



# Эмуляция одно измерение во времени

Для начала Создадим список флюменов для модели `D`:

```
f₁ [ x0, a, x1 ] # прямая связь: из x0 можно перейти в x1
f₂ [ x1, 0, x0 ] # обратная связь: из x1 можно перейти в x0
```
Это означает что две точки x0 и x1 связаны друг с другом и пермещение 
кванта информации `a` возможно между ними. 

Отрисуем шаги:
```
шаг 0: x0-1->x1
шаг 1: x1-1->x0
шаг 2: x0-1->x1
```

Фактически применяя флюмен функцию `D` мы получили элементарный осцилятор.
Он динамичен во времени (тик) и изменяет состояние системы.

Важно: время здесь — не свойство графа, а номер шага применения D. Сам граф 
статичен, меняются только веса.


Теперь опишем топологию флюменов для функции `S`. Этот список флюменов 
представляет собой блок-граф для двух точек (x0, x1) и четырех моментов времени 
(t0–t3). При этом необходимо учитывать что это стартовое состояние до применения `S`,
так сказать "момент творения".

```
    # информация движется во времени для точки x0
    f₁ [ t0x0, a, t1x0 ]
    f₂ [ t1x0, 0, t2x0 ]
    f₃ [ t2x0, 0, t3x0 ]

    # информация движется во времени для точки x1
    f₄ [ t0x1, 0, t1x1 ]
    f₅ [ t1x1, 0, t2x1 ]
    f₆ [ t2x1, 0, t3x1 ]

    # информация движется во времени и перемещается в пространстве (x0 → x1)
    f₇ [ t0x0, 0, t1x1 ]
    f₈ [ t1x0, 0, t2x1 ]
    f₉ [ t2x0, 0, t3x1 ]

    # информация движется во времени и перемещается в пространстве (x1 → x0)
    f₁₀ [ t0x1, 0, t1x0 ]
    f₁₁ [ t1x1, 0, t2x0 ]
    f₁₂ [ t2x1, 0, t3x0 ]
```

Что мы видим:

Все состояния существуют одновременно — метки вида `tixj` уже включают и 
время, и пространство. Никакой эволюции нет, есть только структура.

Обртим внимание на типы связей, при этом понимаем что флюмены неразличимы:

Чисто временные (f₁–f₆) — точка остается той же, время сдвигается.
Пространственно-временные (f₇–f₁₂) — меняется и точка, и время.

Квант информации a пока только в f₁ (t0x0 → t1x0). Все остальные веса — 0.

Что дает эта топология:

1. Любой путь кванта по графу — это возможная траектория в пространстве-времени.
2. Все траектории уже заложены в структуре — нужно только выбрать начальное 
условие.
3. Функция `S` не порождает движение, а фиксирует согласованность распределения.
4. Категорически невозможно перемещение в "пространстве" `x` без перемещения во 
"времени".
5. Для кванта информации у каждого флюмена существуют равноправные альтернативные
пути, что требует определение алгоритма их разрешения.



# Общее правило выбора пути

Рассмотрим развилку в нашем графе. Из точки t1x0 исходят два флюмена:

f₂ [ t1x0, 0, t2x0 ] — остаться в точке x0 но сдвинуться по "временной" шкале;
f₈ [ t1x0, 0, t2x1 ] — перейти в точку x1 и сдвинуться по временной шкале.

Учитывая идентичность флюмена для обоих модей `S` и `D` для обоих моделей 
допустимо опциональное введение следующего общего топологического правила:

1. квант выбирает путь, ведущий в вершину с большим числом входящих связей. Это 
создает эффект "притяжения" к более связанным узлам.

> ❓ Требует обуждения.



# Выбор пути при прочих равных условиях

Необходимо разрешение фундаментального вопроса: при прочих равных 
обстоятельствах, каким образом определить маршрут дальнейшего распространения 
информации?

При этом выбор происходит между идентичными вариантами, ведущими к разным 
последствиям, но локальность запрещает нам заглядывать вперед.

Формально

Пусть квант информации находится во флюмене перед меткой метке `x ∈ 𝕃`. 
Из x исходят два флюмена:
```
f₁[x, w, y₁]
f₂[x, w, y₂]
```

где `y₁ ≠ y₂`, и оба флюмена имеют идентичный вес `w`.
Согласно правилам, квант должен перейти в один из них, но:

w(f₁) = w(f₂) = w (веса равны)

Никакой дополнительной информации о y₁ и y₂, доступной в момент выбора, нет 
(локальность). Дльнейшие связи графа в целом (включая будущие пути из y₁ и y₂) 
известна только в модели `S`, но не может быть использована в момент выбора

Вопрос: каким образом определяется, в какой из флюменов f₁ или f₂ перейдет квант?


# Возможные подходы к разрешению

1. Случайный выбор - Квант с равной вероятностью выбирает любой из доступных 
путей. Это вносит в модель фундаментальную случайность, что требует расширение 
модели новым механизмом, как следствие представление самой сути случайности.

2. Детерминированный выбор - Квант подчиняется жесткому правилу (например, 
приоритет пространственных переходов, направление по индексу и т.п.). Однако при 
прочих равных (одинаковые приоритеты) правило не дает ответа — требуется 
дополнительный критерий или возврат к случайности.

3. Расщепление (квантовый подход) — квант следует по всем путям одновременно. В 
точках ветвления его амплитуда делится между направлениями. Подход требует 
замены w ∈ ℝ на w ∈ ℂ, где |w|² интерпретируется как вероятность обнаружить 
квант в данном флюмене.



# Вывод
Для текущей версии модели вопрос выбора пути при прочих равных остается 
открытым. Это фундаментальная точка развития модели. Вариант ращепления видится 
наиболее перспективным.  Однако реализация этого подхода требует отдельного 
исследования и выходит за рамки данной работы включая:
1. проработку механизма коллапса
2. определенеие причины коллапаса
3. опредление сути фазы
и прочее.





L — счётное множество меток
W ∈ ℂ | ℕ
F ⊆ L × W × L
f = (a, w, b), a, b ∈ L, w ∈ W

f_ℕ^1((a, w₁, b), (c, w₂, b)) = [b; [w₁, w₂]; d]      // A
f_ℂ^1((a, w₁, b), (c, w₂, b)) = [b; [w₁, w₂]; d]      // B
f_ℕ^2((a, w₁, b), (c, w₂, b)) = [b; w₁; d][b; w₂; d]  // C
f_ℂ^2((a, w₁, b), (c, w₂, b)) = [b; w₁; d][b; w₂; d]  // D
f_ℕ^3((a, w₁, b), (c, w₂, b)) = [b; w₁+w₂; d]         // E
f_ℂ^3((a, w₁, b), (c, w₂, b)) = [b; w₁+w₂; d]         // F

x∈{A,B,C,D,E,F}

дай ответ в один символ для эмуляции мультиверса
