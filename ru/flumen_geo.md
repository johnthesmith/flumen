# Вступление

Статья демонстрирует возможность появления эмерджентных свойств аналогичных пространству и 
времени из плоских списков. Показывает, что минимальная непротиворечивая метрика 
дискретного пространства-времени требует отношения 3:4 между временными и 
пространственными шагами.

Текст фиксирует промежуточный этап модели. Вопросы динамики и распространения 
информации требуют отдельного рассмотрения.



# Дисклеймер

Статья — игровая песочница, а не теория всего. Мы исследуем, какие структуры 
возникают из плоского списка с минимальными правилами. Физические термины 
(время, пространство, метрика) используются только для удобства и не претендуют 
на описание реальности.



# Определения

Начинаем с определений:
- [Метка](#метка)
- [Флюмен](#флюмен)
- [Множество флюменов](#множество-флюменов)



## Метка

Метка — элемент счетного множества 𝕃, не имеющий структуры.

Метки не упорядочены и не наделены числовыми или геометрическими свойствами. 
Служат исключительно для различения объектов и установления связей между 
флюменами.



## Флюмен

> ⚠️  Переопределение понятия, введенного в [первой 
> статье](file:///home/still/downloads/flumen_ru.pdf). Далее используется 
> следующее определение:

Флюмен — абстрактная однонаправленная связь между двумя метками `a, b ∈ 𝕃`.

### Обозначение

```
f[a, b]
```

1. a ∈ 𝕃 — метка начала
2. b ∈ 𝕃 — метка конца

> ℹ️  В примерах для наглядности могут использоваться композитные буквенные и 
> индексные обозначения (a, b, c, x₀, t₁), но они всегда являются элементами 𝕃 и 
> не несут числового смысла. Флюмен не имеет координат и не вложен в 
> пространство.


### Свойства флюмена

1. Ориентированность — флюмен f[a, b] и f[b, a] различны при a ≠ b
2. Композиционность — для любых флюменов f[a, b] и f[b, c] существует составной 
путь `a → b → c`.
3. Квантованность — флюмен f[a, b] неделим: существует только как полный кортеж 
`(a, b)`.



## Множество флюменов

Состояние системы — конечное множество флюменов. Отображается
плоским списком, содержащим перечень кортежей флюменов.

```
𝔽 = { f₁[a₁, b₁], f₂[a₂, b₂], …, fₙ[aₙ, bₙ] }
```



### Свойства множества

1. Полнота — множество содержит все флюмены, существующие в данный момент 
рассмотрения.
0. Дискретность — множество флюменов конечно.



> ⚠️ Важно! Множество флюменов формально эквивалентно ориентированному графу. В 
>дальнейшем мы избегаем термина «граф», чтобы подчеркнуть: никакой геометрии не 
>предполагается. Это плоский список пар меток. Структуры (пути, связность) 
>возникают из совпадения меток, а геометрические свойства — результат, а не 
>предпосылка.



# Эмерджентные пространство и время

Опишем множество флюменов:

```
f₁  [a₁, a₂]   f₂  [a₂, a₃]   f₃  [a₃, a₄]
f₄  [b₁, b₂]   f₅  [b₂, b₃]   f₆  [b₃, b₄]
f₇  [a₁, b₂]   f₈  [a₂, b₃]   f₉  [a₃, b₄]
f₁₀ [b₁, a₂]   f₁₁ [b₂, a₃]   f₁₂ [b₃, a₄]
```

На основе композитности интерпретируем, что флюмены образуют три момента времени 
для двух пространственных точек.

При этом флюмены:

- `f₁, f₂, f₃, f₄, f₅, f₆` — движение во времени;
- `f₇, f₈, f₉, f₁₀, f₁₁, f₁₂` — движение в пространстве. 

Таким образом:

1. Любой путь, это возможная траектория в "пространстве-времени".
0. Все потенциальные траектории уже заложены в структуре.
0. Категорически невозможно перемещение в "пространстве" без перемещения во 
"времени".
0. В точках ветвления возникают равноправные альтернативные пути (их выбор
не принципиален для настоящей статьи и рассматрвиается отдельно).



# Метрика времени-пространства

Для модели определим метрику времени пространства как отношение количества 
флюменов, необходимых для пространственного перехода, к количеству флюменов для 
временного перехода:

```
μ = σ / τ
```

где: `τ, σ ∈ ℕ` — целые положительные числа

- `τ` — число флюменов при временном шаге;
- `σ` — число флюменов при пространственном шаге.

В описанной структуре `τ = 1`, `σ = 1`, следовательно `μ = 1` — время и 
пространство равны.

При изменении топологии (например, пространственный переход через несколько 
флюменов) μ может стать больше 1, задавая анизотропию пространства-времени.



# Геометрическая интерпретация

Допустим в модели присутствует наблюдатель воспринимающий пространство в рамках 
евклидовой геометрии.

Приэтом модель дискретна и любой путь измеряется целым числом флюменов.

Рассмотрим два события: A и B. Пусть они разделены `τ` шагов по времени и `σ` шагов 
по пространству.

```
Простраснтво (x)
      ↑
      |        B
      |        *(x+σ, t+τ)
      |       / \
      |      /   \
      |     /     \
      |    /       \
      |   /         \
      |  /           \
      | /             \
      |/               \
A(x,t)*-----------------*A1(x, t+2τ)
      |      сигнал      |
      |  A→B и B→A'      |
      |                  |
      +----------------------→ Время (t)
```

Внутренний наблюдатель в `A` может определить "расстояние" до `B` на основе пути 
`A → B → A1`. Этот путь состоит из двух диагоналей при движении с максимальной 
скоростью.

С точки зрения наблюдателя, время между `A` и `A1` по указанному пути равно 
`2√(τ² + σ²)` (в единицах флюменов). При этом время он измеряет числом 
пройденных флюменов, которое обязано быть целым по шкале `t`.



# Задача

Для согласованной модели, `√(τ² + σ²)` должно быть целым числом.
Отсюда условие `τ² + σ²` должно быть полным квадратом.

Однако при ранее показанной метрике времени-пространства `μ = 1` результат не 
будет целым числом.

```
τ = 1, σ = 1
√(τ² + σ²) = √(1 + 1) = √2 ∉ ℕ
```

Внутренний наблюдатель получает нецелое число квантов (флюменов) — абсурд с 
точки зрения внешенго наблюдателя.

Значит ранее показанная метрика 1:1 не даст внутреннему наблюдателю использовать 
привычныую для него евклидову геометрию в дискретной модели с целыми числами.

Нужны такие `τ, σ`, чтобы `√(τ² + σ²)` было целым, при это `τ, σ` должны быть 
минимальными для минимизации списка флюменов. Достичь этого можно изменив цену 
для "времени" и "пространства". Но так как флюмен элементарен и не содержит 
геометрических понятий, мы можем изменить количество флюменов в между точками 
персечения. При этом мы по прежнему оперируем дискретным квантами.




# Подбор

Решим задачу подборм ближайших минимальных значений:

```
τ = 1, σ = 1 → τ/σ = 1,     √(1² + 1²) = √2 ∉ ℕ
...
τ = 3, σ = 4 → τ/σ = 3/4,   √(9 + 16) = 5 ∈ ℕ ✅
```

таким образом в модели:
- минимальная цена времени 3 флюмена (кванта);
- минимальная цена пространства 4 флюмена (кванта);
- метрика времени пространства 0.75.




# Вывод

Эмерджентное время-пространство возникающее, но явно не заданное в плоском 
списке ориентированных пар (флюменов) допускает непротиворечивую евклидову 
интерпретацию.

Если в дискретной модели требовать, чтобы евклидова диагональ d = √(τ² + σ²) 
измерялась целым числом, то параметры `τ`, `σ` обязаны образовывать пифагорову 
тройку. Минимальное нетривиальное решение этого условия — (3,4,5).

Перемещение «вдоль времени» и «вдоль пространства» не ломает целочисленность 
диагоналей, и модель остаётся внутренне непротиворечивой. Минимальная 
согласованная анизотропия временного и пространственного шага в такой модели 
равна 3:4.

При увеличении размерности простнатсва на основе представленной структуры можно 
построить фигуры, аналогичные окружностям, где отношение длины окружности к 
диаметру постепенно сходится к числу, близкому к `π`. Следовательно, дискретные 
флюмены способны порождать иррациональные константы, как эмерджентное свойство 
внутри модели, без явного задания их априори.

Все перечисленное не необходимость «мира», а минимальное условие целочисленной 
евклидовой совместимости в дискретной структуре.
